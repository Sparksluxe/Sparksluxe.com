# Sparksluxe.com
Sparksluxe demo e-commerce 
{
"name": "sparksluxe-full-demo",
"version": "1.0.0",
"private": true,
"scripts": {
"start": "react-scripts start",
"build": "react-scripts build"
},
"dependencies": {
"react": "18.2.0",
"react-dom": "18.2.0",
"react-scripts": "5.0.1",
"react-router-dom": "6.14.1"
}
}
import React, {useState,useEffect} from 'react';
import { BrowserRouter as Router, Routes, Route, Link, useNavigate } from 'react-router-dom';
const PRODUCTS = [{id:'p1',title:'Gold-Plated Necklace',price:45000,img:'https://picsum.photos/seed/g1/800/600'},{id:'p2',title:'Diamond Ring',price:250000,img:'https://picsum.photos/seed/g2/800/600'}];
function Header({cartCount,user}){return (<header className='header'><div style={{display:'flex',gap:12,alignItems:'center'}}><div className='brand'>Sparks-Luxe</div></div><nav><Link to='/' style={{color:'#fff',marginRight:12}}>Home</Link><Link to='/collections' style={{color:'#fff',marginRight:12}}>Collections</Link><Link to='/login' style={{color:'#fff',marginRight:12}}>Login</Link><Link to='/cart' style={{color:'#fff'}}>Cart ({cartCount})</Link></nav></header>);}function Home(){return (<div className='container'><div className='card' style={{padding:20}}><h2>Flash Sale – Up to 30% Off!</h2><p>Shop our selected deals.</p></div><div style={{height:12}}></div><div className='card'><h3>Hot Deals</h3><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:12,marginTop:12}}>{PRODUCTS.map(p=>(<div key={p.id} className='card'><img src={p.img} style={{width:'100%',height:160,objectFit:'cover',borderRadius:6}}/><div style={{fontWeight:800,marginTop:8}}>{p.title}</div><div style={{color:'#666'}}>₦{p.price.toLocaleString()}</div><div style={{marginTop:8}}><Link className='button' to={'/product/'+p.id}>View</Link></div></div>))}</div></div></div>);}function Collections(){return (<div className='container'><h2>Collections</h2></div>);}function Product({id, addToCart}){const p=PRODUCTS.find(x=>x.id===id); if(!p) return <div className='container card'>Not found</div>; return (<div className='container card' style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:20}}><div><img src={p.img} style={{width:'100%',borderRadius:8}}/></div><div><h2>{p.title}</h2><div style={{fontWeight:800,fontSize:20}}>₦{p.price.toLocaleString()}</div><div style={{marginTop:12}}><button className='button' onClick={()=>addToCart(p)}>Add to cart</button></div></div></div>);}function Cart({cart,updateQty,removeItem}){const subtotal=cart.reduce((s,i)=>s+i.qty*i.price,0); return (<div className='container'><h2>Your Cart</h2>{cart.length===0? <div className='card'>Empty</div>: <div className='card'>{cart.map(it=>(<div key={it.id} style={{display:'flex',gap:12,alignItems:'center',padding:8,borderBottom:'1px solid #eee'}}><img src={it.img} style={{width:80,height:80,objectFit:'cover'}}/><div style={{flex:1}}>{it.title}<div style={{color:'#666'}}>₦{it.price.toLocaleString()}</div></div><div><button onClick={()=>updateQty(it.id,it.qty-1)}>-</button><span style={{margin:'0 8px'}}>{it.qty}</span><button onClick={()=>updateQty(it.id,it.qty+1)}>+</button></div><div style={{marginLeft:12}}>₦{(it.qty*it.price).toLocaleString()}</div></div>))}<div style={{textAlign:'right',marginTop:12}}>Subtotal: <strong>₦{subtotal.toLocaleString()}</strong><div style={{marginTop:8}}><Link className='button' to='/checkout'>Checkout</Link></div></div></div>}</div>);}function Checkout({cart,clearCart,createOrder,navigate}){const [form,setForm]=useState({name:'',email:'',address:'',method:'flutterwave'});const subtotal=cart.reduce((s,i)=>s+i.qty*i.price,0);function place(){const order=createOrder({...form,amount:subtotal});clearCart();alert('Order placed - demo');navigate('/order/'+order.id);}return (<div className='container'><h2>Checkout</h2><div className='card'><input placeholder='Name' style={{width:'100%',padding:8}} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder='Email' style={{width:'100%',padding:8,marginTop:8}} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><textarea placeholder='Address' style={{width:'100%',padding:8,marginTop:8}} value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><div style={{marginTop:8}}>Delivery: <strong>1-7 working days</strong></div><div style={{marginTop:12,textAlign:'right'}}><button className='button' onClick={place}>Place Order</button></div></div></div>);}function OrderView({orders}){const id=window.location.pathname.split('/order/')[1];const order=orders.find(o=>o.id===id);if(!order) return <div className='container card'>Not found</div>;return (<div className='container card'><h2>Order {order.id}</h2><div>Status: <strong>{order.status}</strong></div></div>);}function Login({login}){const [email,setEmail]=useState('');const [pass,setPass]=useState('');const [phase,setPhase]=useState('creds');const [code,setCode]=useState('');const navigate=useNavigate();async function submit(e){e.preventDefault();const res=await login(email,pass);if(res.requires2FA) setPhase('2fa'); else if(res.success){ if(res.role==='super_admin') navigate('/admin/god'); else if(res.role==='sales_admin') navigate('/admin/sales'); else navigate('/');}}async function verify(){const res=await login(email,pass,code); if(res.success){ if(res.role==='super_admin') navigate('/admin/god'); else if(res.role==='sales_admin') navigate('/admin/sales'); else navigate('/'); } else alert('Invalid code'); }return (<div className='container' style={{marginTop:18}}><div className='card' style={{maxWidth:480,margin:'0 auto',padding:18}}><h3>Login</h3>{phase==='creds' && (<form onSubmit={submit}><input placeholder='Email' style={{width:'100%',padding:8}} value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder='Password' type='password' style={{width:'100%',padding:8,marginTop:8}} value={pass} onChange={e=>setPass(e.target.value)}/><div style={{marginTop:12}}><button className='button' type='submit'>Login</button></div></form>)}{phase==='2fa' && (<div><div className='small'>Enter 6-digit code sent to your email (demo)</div><input value={code} onChange={e=>setCode(e.target.value)} style={{width:'100%',padding:8,marginTop:8}} /><div style={{marginTop:8}}><button className='button' onClick={verify}>Verify</button><button style={{marginLeft:8}} onClick={()=>{alert('Backup link sent'); setPhase('creds');}}>Use backup link</button></div></div>)}<div style={{marginTop:12,fontSize:12,color:'#666'}}>Demo admin emails: sparks.owner@example.com, sparks.sales@example.com, sparks.media@example.com</div></div></div>);}function AdminGod({orders,traffic,logout}){const [view,setView]=useState('advanced');return (<div className='container' style={{marginTop:18}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><h2>God Mode</h2><div><button className='switch' onClick={()=>setView(view==='advanced'?'simple':'advanced')}>Switch</button><button onClick={logout} style={{marginLeft:8}} className='switch'>Logout</button></div></div><div style={{height:12}}></div>{view==='advanced'? <div className='card'><div>Advanced analytics stub. Visitors: {traffic.visitors}</div></div>: <div className='card'><div>Simple view. Orders: {orders.length}</div></div>}</div>);}function AdminSales({orders}){return (<div className='container' style={{marginTop:18}}><h2>Sales</h2><div className='card'>{orders.map(o=>(<div key={o.id}>{o.id} — {o.status} — ₦{o.total.toLocaleString()}</div>))}</div></div>);}function AdminMedia({products}){return (<div className='container' style={{marginTop:18}}><h2>Media</h2><div className='product-grid'>{products.map(p=>(<div key={p.id} className='card product'><img src={p.img} alt=''/><div style={{fontWeight:800}}>{p.title}</div></div>))}</div></div>);}export default function App(){const [cart,setCart]=useState([]);const [orders,setOrders]=useState([]);const [user,setUser]=useState(null);const [traffic]=useState({visitors:1245});const navigate=useNavigate();useEffect(()=>{localStorage.setItem('sl_cart',JSON.stringify(cart));},[cart]);useEffect(()=>{const u=localStorage.getItem('sl_user'); if(u) setUser(JSON.parse(u));},[]);function addToCart(p){setCart(prev=>{const ex=prev.find(x=>x.id===p.id); if(ex) return prev.map(x=> x.id===p.id? {...x, qty:x.qty+1}:x); return [...prev, {...p,qty:1}];}); alert('Added to cart (demo)');}function updateQty(id,qty){setCart(prev=> prev.map(x=> x.id===id? {...x, qty: Math.max(1, qty)}:x));}function removeItem(id){setCart(prev=> prev.filter(x=> x.id!==id));}function clearCart(){setCart([]);}function createOrder({name,email,address,amount,method}){const id='SL'+Math.floor(1000+Math.random()*9000);const order={id,name,email,address,paymentMethod:method,status:'Pending',items:cart.map(c=>({id:c.id,title:c.title,qty:c.qty,price:c.price})),total:amount};setOrders(prev=>[order,...prev]);console.log('Notify admin at sparksluxe.service@gmail.com');return order;}async function login(email,password,code){let role='customer';if(email==='sparks.owner@example.com') role='super_admin';else if(email==='sparks.sales@example.com') role='sales_admin';else if(email==='sparks.media@example.com') role='media_admin';const deviceKey=window.location.hostname+'_trusted';const trusted=localStorage.getItem(deviceKey)==='1';if((role!=='customer') && !trusted){if(!code) return {requires2FA:true, role}; if(code.length!==6) return {success:false}; localStorage.setItem(deviceKey,'1');}const u={email,role};setUser(u);localStorage.setItem('sl_user',JSON.stringify(u));return {success:true, role};}function logout(){setUser(null);localStorage.removeItem('sl_user');navigate('/');}return (<Router><div><Header cartCount={cart.reduce((s,i)=>s+i.qty,0)} user={user} /><Routes><Route path='/' element={<Home/>} /><Route path='/collections' element={<Collections/>} /><Route path='/product/:id' element={<ProductRoute addToCart={addToCart} />} /><Route path='/cart' element={<Cart cart={cart} updateQty={updateQty} removeItem={removeItem} />} /><Route path='/checkout' element={<Checkout cart={cart} clearCart={clearCart} createOrder={createOrder} navigate={navigate} />} /><Route path='/order/:id' element={<OrderView orders={orders} />} /><Route path='/login' element={<Login login={login} />} /><Route path='/admin/god' element={<Require role='super_admin' user={user}><AdminGod orders={orders} traffic={traffic} logout={logout} /></Require>} /><Route path='/admin/sales' element={<Require role='sales_admin' user={user}><AdminSales orders={orders} /></Require>} /><Route path='/admin/media' element={<Require role='media_admin' user={user}><AdminMedia products={PRODUCTS} /></Require>} /><Route path='/survey' element={<div className='container' style={{marginTop:18}}><div className='card'>Visitors: {traffic.visitors}</div></div>} /><Route path='*' element={<div className='container' style={{marginTop:18}}>Page not found</div>} /></Routes><footer className='footer'><div style={{padding:12}}>© Sparks-Luxe • <a href='mailto:sparksluxe.service@gmail.com'>sparksluxe.service@gmail.com</a> • <a href='https://facebook.com' target='_blank' rel='noreferrer'>@sparks-luxe</a></div></footer><a className='fab' href={process.env.REACT_APP_WHATSAPP_URL || 'https://wa.me/2348154129399'} target='_blank' rel='noreferrer'>💬</a></div></Router>);}function ProductRoute({addToCart}){const id=window.location.pathname.split('/product/')[1];return <Product id={id} addToCart={addToCart} />}function Require({role,user,children}){if(!user) return <div className='container card' style={{marginTop:18}}>Please <Link to='/login'>login</Link> to access</div>; if(user.role!==role) return <div className='container card' style={{marginTop:18}}>Access denied</div>; return children; }
import React from 'react';import { createRoot } from 'react-dom/client';import App from './App';import './styles.css';const root = createRoot(document.getElementById('root'));root.render(<App/>);